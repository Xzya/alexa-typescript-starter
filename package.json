{
  "name": "alexa-typescript-starter",
  "version": "1.0.0",
  "description": "Alexa TS starter",
  "main": "index.js",
  "scripts": {
    "clean": "rimraf dist",
    "build": "npm run clean && gulp",
    "deploy": "npm run build && npm run test:coverage && ask deploy",
    "deploy:lambda": "npm run build && ask deploy --target lambda",
    "deploy:local": "ask deploy --target skill --profile local",
    "test": "jest",
    "test:watch": "jest --watch",
    "test:coverage": "jest --coverage",
    "start": "nodemon"
  },
  "jest": {
    "testEnvironment": "node",
    "transform": {
      "^.+\\.tsx?$": "ts-jest"
    },
    "moduleFileExtensions": [
      "ts",
      "tsx",
      "js"
    ],
    "testMatch": [
      "**/*.spec.ts"
    ],
    "coverageDirectory": ".coverage",
    "coveragePathIgnorePatterns": [
      "/__tests__/*",
      "lambda/locales/"
    ]
  },
  "author": "Mihail Cristian Dumitru",
  "contributors": [
    "Guilherme Caixeta"
  ],
  "license": "MIT",
  "dependencies": {
    "ask-sdk-core": "^2.8.0",
    "ask-sdk-model": "^1.28.0",
    "i18next": "^19.5.1",
    "i18next-fs-backend": "^1.0.6",
    "lodash": "^4.17.15"
  },
  "devDependencies": {
    "@types/express": "^4.17.6",
    "@types/jest": "^26.0.3",
    "@types/lodash": "^4.14.157",
    "@types/node": "^14.0.14",
    "event-stream": "^4.0.1",
    "express": "^4.17.1",
    "gulp": "^4.0.2",
    "gulp-rename": "^2.0.0",
    "gulp-typescript": "^5.0.1",
    "jest": "^26.1.0",
    "nodemon": "^2.0.4",
    "rimraf": "^3.0.2",
    "ts-jest": "^26.1.1",
    "ts-node": "^8.10.2",
    "typescript": "^3.9.5"
  }
}
